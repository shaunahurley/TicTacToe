package Game;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Random;
import java.util.Scanner;

public class TicTacToe
{
    static ArrayList<Integer> playerPositions;
    static ArrayList<Integer> cpuPositions;
    
    public static void main(String[] args)
    {
        public static void playTicTactToe()
        {
            char[][] board = {{' ', '|', ' ', '|', ' '},
                              {'-', '+', 
            
            Scanner scn = new Scanner(System.in);
            
            System.out.println("Where do you wanna place 'X'? Enter position [1-9]");
            int userPos = scn.nextInt();
            
            while(!valivPos(userPos))
            {
                System.out.println("position taken, try another position");
                userPos = scn.nextInt();
            }
            placePiece(board, userPos, "player");
            String res = checkWin();
            if(res.length() > 0)
            {
                printBoard(board);
                System.out.println(res);
                break;
            }
       }
   }
   public static boolean validPos(int pos)
   {
    if(playerPosition.contains(pos) || cpusPositions.contains(pos))
    {
        return false;
    }
    return true;
 }
 
 ppublic static void printBoard(char[][] board)
 {
    
}
